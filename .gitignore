# Bulk scraped data (regenerable via download_am730_column.py / migrate_to_sqlite.py)
articles_*/
articles_metadata.csv
yearly_top_tags.csv
download_all_years.log

# Article browser site (added to as the plan's tasks build it)
# articles.db is intentionally tracked (checked in) — file-based DB, so the
# repo/Docker image is self-contained without a separate bootstrap step.
venv/
__pycache__/
*.pyc
frontend/node_modules/
frontend/dist/

# Cached TTS audio (regenerable via tts.py; wiped on every redeploy anyway,
# see pregenerate_audio.py)
tts_cache/

.DS_Store
.env

# Git worktrees used for isolated feature work
.worktrees/
